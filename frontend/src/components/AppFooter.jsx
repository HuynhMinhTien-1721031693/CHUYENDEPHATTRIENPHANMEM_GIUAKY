export default function AppFooter() {
  return (
    <footer className="app-footer" role="contentinfo">
      <p>
        Backend cổng 3001 (WebSocket + đăng nhập). <strong>Admin phòng live</strong> và{" "}
        <strong>Quản lý hệ thống</strong> là hai quyền tách biệt — xem <code>.env.example</code> ở backend để tạo tài
        khoản mẫu.
      </p>
    </footer>
  );
}
